// Generated per-environment by `make dev` / `make deploy` from GMAIL_PROXY_URL in .env.
// Copy to env.js (or run `make sync`) and edit locally. env.js itself is gitignored.
window.__FINCOACH_CONFIG__ = {
  GMAIL_PROXY_URL: "https://your-worker.your-subdomain.workers.dev",
};
