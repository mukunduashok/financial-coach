# Cloudflare Worker URL for Gmail OAuth. Used by `make dev` and `make deploy`.
# Copy to .env and set your own value. .env is gitignored.
GMAIL_PROXY_URL=https://your-worker.your-subdomain.workers.dev
